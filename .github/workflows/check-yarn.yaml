name: Check - Yarn and dependencies
on:
  pull_request:
    paths:
      - ".github/workflows/check-yarn.yaml"
      - "package.json"
      - "yarn.lock"
  workflow_dispatch:
permissions:
  contents: read

jobs:
  wf:
    uses: nikobockerman/github-workflows/.github/workflows/check-yarn.yaml@4f72abc1d57a1a8c8d2741d5b2dc84861eddb0cf
